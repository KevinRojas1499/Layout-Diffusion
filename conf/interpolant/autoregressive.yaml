defaults:
  - base_interpolant

name: autoregressive
# These fields exist on the shared schema but the autoregressive interpolant
# does not consume them; they're set to defaults so config validation passes.
dsm_t_reweight: false
cfg_dropout_prob: 0.0
cat_cond_prob: 0.0
size_cond_prob: 0.0
