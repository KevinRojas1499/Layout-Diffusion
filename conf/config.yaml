# @package _global_
# Layout training config - compose from config groups
# Override from CLI: python layout_training.py run.batch_size=64 model.hidden_dim=128

defaults:
  - _self_
  - run: default
  - dataset: publaynet
  - model: transformer
  - optimizer: adamw
  - interpolant: multimodal
  - eval: default

# Root structure - config groups merge into these keys
run: {}
dataset: {}
model: {}
optimizer: {}
interpolant: {}
eval: {}
