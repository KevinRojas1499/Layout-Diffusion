defaults:
  - base_dataset

name: publaynet
max_length: 20
split: train
max_samples: null
# Directory containing the LayoutFlow h5 splits (publaynet_{train,val,test}.h5).
# Populate with: `git clone https://huggingface.co/JulianGuerreiro/LayoutFlow`
# and point at `<that-clone>/dataset/publaynet`.
data_path: /workspace/LayoutFlow-data/dataset/publaynet
