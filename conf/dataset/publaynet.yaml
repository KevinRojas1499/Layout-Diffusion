defaults:
  - base_dataset

name: publaynet
max_length: 25
split: train
max_samples: null
# data_path: path to annotations-only dir (train.json, val.json) to avoid ~130GB image download.
# Run extract_annotations_to_json() once, upload the output dir, then set this path.
data_path: null  # e.g. "data/publaynet_labels"
